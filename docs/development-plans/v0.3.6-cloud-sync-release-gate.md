# v0.3.6 云同步 MVP 收口与合并准备

## 目标

收口 v0.3 Cloud Sync MVP，确认本分支已经具备合并到 `develop/master` 前的最小验证材料。

## 已完成范围

```text
0.3.1 Cloud API /sync/push 同步写入接口
0.3.2 PowerSync credentials 与开发期最小身份
0.3.3 Flutter uploadData -> /sync/push
0.3.4 Settings PowerSync 连接编排与诊断入口
0.3.5 后端同步 smoke 脚本与验证说明
0.3.6 release gate 检查脚本与收口说明
```

## 当前可验证闭环

```text
后端可签发 PowerSync credentials
后端可接收 memo/task/expense 同步 push
Flutter 可把 PowerSync CRUD batch 映射为 /sync/push payload
Flutter Settings 可手动连接 / 断开 PowerSync 并刷新上传诊断
脚本可验证 credentials + sync push + REST readback + stale revision
```

## v0.3 明确未收入口

```text
生产级账号体系
复杂冲突解决
端到端加密
团队共享权限
附件二进制同步
导入导出批量同步
真实多设备自动化测试矩阵
PowerSync rules 在线环境自动创建
```

以上进入后续版本或环境部署任务。

## 合并前检查命令

核心离线检查：

```bash
bash scripts/check-v0.3-sync.sh
```

后端 API 已启动时的 live smoke：

```bash
bash scripts/smoke-sync-v0.3.sh
```

Flutter 页面手动验证：

```text
设置 -> 同步凭据 / PowerSync -> 连接 PowerSync
```

连接后进行 memo/task/expense 本地写入，再刷新同一卡片，查看 uploadData 上传状态与错误摘要。

## 当前合并状态

```text
分支：feat/v0.3-cloud-sync-mvp
目标：develop/master
状态：核心 release gate 检查已通过；live API smoke 已通过
```

## 进入下一版本前动作

```text
1. 合并 feat/v0.3-cloud-sync-mvp 到 develop/master
2. 在合并点打 tag：v0.3.0
3. 在合并点打 tag：v0.3.0
4. 删除 v0.3 功能分支
5. 基于 develop/master 开启 v0.4 版本分支
```
