# Lifly v0.2.8 Release Gate

## 版本

```text
0.2.8
```

## 当前分支

```text
feat/v0.2-local-data-mvp
```

## 目标

确认 v0.2.x Local Data MVP 已达到发布门禁：本地数据底座、memo/task/expense 本地闭环、Flutter 本地模式、重启持久化回归和 Local MCP 兼容性均通过验证。

## 已执行检查

### Flutter 客户端

```text
flutter pub get：通过
需要说明：flutter analyze 被工具安全检查拦截，改用 dart analyze

dart analyze：通过，No issues found
flutter test：通过，All tests passed，27 tests
```

原计划脚本：

```bash
bash ./scripts/check-client-flutter.sh
```

该脚本在本轮执行时超时，未返回完整结果；已按脚本内容拆分为 `flutter pub get`、`dart analyze`、`flutter test` 三段验证。

### TypeScript / Local MCP

新增并执行脚本：

```bash
bash ./scripts/check-local-mcp.sh
```

结果：

```text
pnpm typecheck：通过，3 packages
pnpm test：通过，3 packages，19 tests
Local MCP smoke：通过，15 checks
```

### Git 检查

```text
git diff --check：通过
工作区：干净
```

## v0.2.x 已完成能力

```text
PowerSync Local Core 基础底座
LocalCoreContext / id / timestamp / revision 策略
本地写入事务执行器
本地 audit_logs 写入
memo create/search/update/delete
memo soft delete / revision / audit_logs
memo Flutter 页面 Local Core 模式
memo 重启持久化回归

task create/list/update/complete/delete
task todo/done 过滤
task completed_at / soft delete / revision / audit_logs
task Flutter 页面 Local Core 模式
task 重启持久化回归

expense create/search/summary/delete
expense amount > 0 校验
expense soft delete / revision / audit_logs
ledger Flutter 页面 Local Core 模式
expense 重启持久化回归

settings 显示当前数据模式
repository local mode 测试覆盖
Local MCP v0.1 smoke 未被破坏
```

## 当前边界

```text
不包含 Cloud Sync
不包含 PowerSync uploadData
不包含跨设备同步
不包含生产账号体系
不包含端到端加密
不包含 Local MCP 到 Flutter 真实进程桥接
ledger update 在本地模式下暂未实现，会明确返回 UnsupportedError
```

## 发布建议

```text
1. 合并 feat/v0.2-local-data-mvp 到 master
2. 在 master 合并点打 tag：v0.2.8
3. 删除 feat/v0.2-local-data-mvp
4. 新建 v0.3.0 Cloud Sync MVP 分支
```

## v0.3 入口

下一版本建议进入：

```text
v0.3.0 Cloud Sync MVP
```

核心范围：

```text
PowerSync uploadData
Cloud API 同步写入 endpoint
登录/token 基础接入
跨设备数据可见
同步状态诊断
最小冲突策略
```
